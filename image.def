Bootstrap: docker
From: alpine:latest

%labels
    AUTHOR Quentin Festor
    VERSION 1.0
    DESCRIPTION "test de création de sif pour computo"

%startscript


%setup
    cd /
    touch .condarc

%post
    apk add wget bzip2 bash curl git
    wget -O environment.yml https://raw.githubusercontent.com/Qufst/create_apptainer.sif/main/environment.yml
    wget -c https://micro.mamba.pm/install.sh
    /bin/bash install.sh -bfp /usr/local
    micromamba create -y -n myenv -f environment.yml

%environment
    micromamba activate myenv

%help
    Container with micromamba and myenv

