Bootstrap: docker
From: alpine:latest

%labels
    AUTHOR Quentin Festor
    VERSION 1.0
    DESCRIPTION "test de création de sif pour computo"

%post
    apk add wget tar bzip2 bash curl
    ls -la
    wget -O opt/environment.yml https://raw.githubusercontent.com/Qufst/create_apptainer.sif/main/environment.yml
    chmod -R ugo+rwx ${SHELL}
    "${SHELL}" <(wget -qO- https://micro.mamba.pm/install.sh)

    micromamba shell hook --shell
    micromamba create -y -n myenv -f opt/environment.yml

%environment
    micromamba activate myenv

%help
    Utilisation : apptainer run image.sif --option

